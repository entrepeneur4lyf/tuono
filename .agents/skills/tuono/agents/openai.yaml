interface:
  display_name: "Tuono"
  short_description: "Repo-specific patterns and workflows for tuono"
  default_prompt: "Use the tuono repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true